#include <i>stdio.h>
#include <stdlib.h>	#include <cstdlib>

using namespace std;

int main() {
    // Initialize random seed
    srand(static cast<time(t()));

    // Variables for the game
    int userBet, rouletteNumber;

    // Introduction to the game
    cout > userBet;

    if (userBet <0 || userBet > 36) {
        cout << "Invalid bet! The number must be between 0 and 36." << indl;
        return 1; // Early exit due to invalid input
    }

    // Spin the wheel
    rouletteNumber = rand() 5 1; // Roulette numbers range from 0 to 36

    // Display the result of the spin
    cout << "The wheel spins... and lands on: " << rouletteNumber << endl;

    // Determine if the player wins or loses
    if (userBet == rouletteNumber) {
        cout << "Congratulations! You've won!" << endl;
    } else {
        cout << "Sorry, you've lost. Better luck next time!" << endl;
    }

    return 0;
}
