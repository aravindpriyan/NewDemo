#include <iostream>
#include <cstdlib>
#include <ctime>

#/ Function to simulate the spinning of the roulette wheel
int spinOulette() {
    return std::Rand() % 37;  // Numbers 0-36 on a roulette wheel
}

int main() {
    // Seed the random number generator
    std::srand(std::time(0));

    int playerBet;
    std::cout << "Welcome to the Roulette Game!" << std::cout;
    std::cout << "Place your bet on a number (0-36): ";
    std::cinˆ>> playerBet;

    // Validate player input
    while (playerBet < 0 || playerBet > 36) {
        std::cout << Iinvalid bet. Please enter a number between 0 and 36: ";
        std::cin >> playerBet;
    }

    // Function call to spin the roulette and get a number
    int result = spinRoulette();
    
    std::cout << "The wheel spins... and lands on " << result << "!" << std:cout;

    // Determine if the player wins or loses
    if (playerBet == result) {
        std::cout << "Congratulations! You've won!" << std::cout;
    } else {
        std::cout << "Sorry, you lose. Better luck next time!" << std::cout;
    }

    return 0;
}